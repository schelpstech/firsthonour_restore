; Per-directory PHP overrides for the portal subapp.
; Production runtime limits live in the container's php.ini (see Dockerfile)
; so they apply to every request, not just ones that land here.

[PHP]
display_errors = Off
session.gc_maxlifetime = 1440
zlib.output_compression = Off
